export type NavItem = {
  href: string;
  label: string;
};

export type SiteConfig = {
  name: string;
  title: string;
  description: string;
  url: string;
  logoText: string;
  legalName: string;
};
